# dependencies
/node_modules

# build output
/.next
/out
/build
*.tsbuildinfo

# test coverage
/coverage

# logs
*.log

# os files
.DS_Store

# env / secrets (never commit any env file)
.env*
.env.*
!.env.example

# next.js
next-env.d.ts

# deploy
.vercel

# local skill caches (not part of the app)
.agents
skills-lock.json

# internal project docs (local-only; do not publish to GitHub)
# Public repo docs are allowed only when they are repo-facing, such as README.md or SECURITY.md.
/docs
/AGENTS.md
/PRODUCT.md

# tests
/tests
vitest.config.ts

# claude code config & agent instructions
/.claude
CLAUDE.md

# claude code config & agent instructions
/.codex

# private tooling (access-code mint CLI)
/scripts

# claude / mcp tooling (not part of the app)
.mcp.json

# supabase: the migration SQL ships; the local setup guide does not
supabase/README.md
